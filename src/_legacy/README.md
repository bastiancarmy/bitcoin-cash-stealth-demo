# Legacy files (NOT USED)

Everything in this folder is preserved for reference only.

Do not import from `src/_legacy`.

Canonical implementations live in publishable packages under `packages/@bch-stealth/*`,
and the supported CLI lives in `packages/cli`.